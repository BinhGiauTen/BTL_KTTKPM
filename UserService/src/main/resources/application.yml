eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 9040
 
spring:
  application:
    name: userService
  config:
    import: optional:configserver:http://localhost:8888